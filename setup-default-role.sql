-- Vérifier les rôles existants
SELECT * FROM directus_roles;

-- Si le rôle "Authenticated User" existe, récupérer son ID
-- Puis mettre à jour la configuration
-- Note: Cette requête est à adapter selon votre structure
